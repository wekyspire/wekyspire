# Handoff：three.js 大重构 — Core 层完成，玩法稳固阶段

> 日期：2026-07-31 ｜ 分支：`rewrite` ｜ 最新提交：`1b1d97e`
> 旧代码留底：tag `pre-rewrite-archive`（= master 的 `9fb1bac`）

## 一句话现状

整体重写（无向后兼容）进行中。**Core + Bridge + Stage 基座 + BattleStage 场景组装完成，33 个测试文件 193 用例全绿**，
`npm run dev` 的战斗调试页可在浏览器里完整打一场战斗（拖拽出牌/结束回合/结算期输入/tooltip）。
下一步：Shell 薄壳、Rest 阶段、演出升级。

## 权威文档

- **`quest_prompts/THREE_REFACTOR_PLAN.md`**：重构唯一权威设计依据（v3，含 §11 落地记录，与实现有出入处以 §11 为准）。
- **`src/core/skills/SKILL_DESIGN_PRINCIPLES.md`**：全部技能体系的设计意图（从旧分支找回）。玩法敲定过程中在此文档标注各体系"已验证/已敲定"状态。

## 环境

```bash
npm install        # 依赖：three / vue / vue-router / gsap / mitt / vitest（pixi、snapdom、html2canvas、rfdc 已删除）
npm test           # = npx vitest run，当前 40 个测试文件 257 用例全绿
npm run dev        # 战斗调试页（localhost:5177）：单场战斗前端冒烟，可拖拽出牌
```

注意：`npm run dev` 暂不可用（旧 Vue 前端已整体删除，Shell 未重建）——这是预期状态，重写策略不要求中间产物可运行。

## 已完成清单

1. **仓库手术**（`a707007` 等）：旧 `src/data|components|renderers|webgl|utils` 全部删除，仅留 `src/assets`。
2. **结算内核** `src/core/kernel/`：
   - `BattleInstruction`：`true/false/WAIT` 三返回值、payload 白名单（`setPayload` 越界抛错）、`isAlive()` 父链取消传播。
   - `BattleKernel`：DFS 泵、订阅注册表（PRE 修饰/否决 + POST 反应挂已完成节点、priority 降序+注册序、`battle/turn/once` 窗口、owner 批量注销）、取消三动词（veto 可带替代指令 / abort 沿父链 / inspect=父读子 result）、触发链深度上限 32、`getAbortTarget`、`tracer` 调试钩子。
3. **状态底座** `src/core/state/`：纯对象（**禁 reactive**）。`Unit`（hp/shield/effects/getStat 读轨/uniqueID/side）、`Player`（run 级）、`AIUnit→Enemy/Ally`（多单位！）、`runState`/`battleState`（两层拆分）、zones（hand/deck/discard/burnt + chant 槽，有序）、history（turn/battle 双段）、种子 rng、`skillRuntime`（定义/运行时分离）。
4. **指令族** `src/core/instructions/`：combat（伤害/治疗/护盾）、resources（mana/AP）、effects（AddEffect 兼管效果订阅挂载/注销）、cards（抽/焚/弃，抽空自动洗回）、skill（UseSkill 三阶段/资源消耗/激活/手动停咏唱/冷却推进）、turn（回合标记+玩家/敌方回合阶段机+TurnLoop）、battleRoot（战前/战后/根）、input（AwaitPlayerInput 结算期 WAIT）、aiAct（敌友共用）。
5. **契约+注册表**：`createRegistry` 工厂五表同构（effect/skill/enemy/ally/ability）；skill 契约（sctx={...ctx,self,def}、keywords、charges、cardMode/activated、subscriptions、meta）；效果=数据+statModifiers（读轨）+subscriptions（写轨）。
6. **战斗装配** `src/core/flow/battle.js`：`createBattle/startBattle/playerUseSkill/playerEndTurn/respondInput/...`。回合结构：玩家回合=开始结算→冷却→抽牌（首回合跳过）→**瑞米等 ally 先行动**→WAIT 玩家输入；敌方回合=敌人按数组序行动→预算意图。**终局 abort TurnLoop 而非根**，战后清理在树内执行。
7. **最小内容** `src/core/content/`：4 技能/2 敌人/瑞米/战意/燃烧（`content/index.js` 显式 import 登记，不用 glob）。
8. **Headless SDK** `src/core/sdk/driver.js`：`BattleDriver` 声明式装配 + 链式出牌 + respond/dispatch/calls/runToEnd/trace。
9. **测试** `test/`（64 用例）：kernel/state/instructions/skills/agents/battle/complexBattles（多敌人、眩晕 veto、斩灭 once、呼吸 turn 窗、完美花刀结算期输入、瑞米死亡、燃烧致死）。
10. **玩法敲定首批原型**（`1b1d97e`，+18 用例共 82 绿）：刀组合卡序（bladeCombo）、太极/混元跨回合计数（chantCounting）、火灵脉焚卡+自伤（fireVein）、武者/武帝 PRE 减免优先级（blockMastery）。结论：四体系现有原语全部可表达，Core 无改动；计数器约定放 skillRuntime 不藏闭包。详见下文"下一步"勾选记录。

## 关键约定（踩坑备忘）

- 依赖方向严格单向：Core 不 import Bridge/Stage/Shell；表现诉求只走 `ctx.presenter`（`createNullPresenter`/`createRecordingPresenter`）。
- 牌库顶 = 数组 index 0；首回合不抽牌（起手=initialDraw）；护盾在己方阵回合开始清零。
- 牌的 zone 不显式存储，`zoneOf/moveCard` 反查，数组是唯一事实源（直接 push 会双重属主）。
- history 按**阵营**统计（瑞米输出计入 damageDealt）；死亡单位留在数组里靠 isDead() 过滤。
- 技能触发订阅：战斗开始注册一次（window:'battle'），zone 限定写 filter；咏唱订阅 owner=卡牌，停止时按 owner 注销。
- 效果订阅：AddEffectInstruction 首次获得时挂载（owner=`effect:{unit}:{effect}`），层数扣尽注销。
- 费用管线：技能消耗提交资源指令子节点，PRE 改费天然生效。
- 状态可序列化约束：只放 id/slug+标量，定义引用一律经注册表反查。
- 前后端同步约定（未来 Bridge 用）：字段名 `_` 结尾为后端私有，其余投影。
- 伤害/面板修正三段式（精确版）：
  `amount = 技能基础值 + getStat(面板)` → `payload = PRE 流水线(amount)` → `execute(payload)`（固定公式：减防御→护盾吸收→minHp 地板）。
  - L1 面板区：`getStat(stat, view?)` = base + Σ modᵢ(stacks, unit, view)。合成只加性（可交换），但单个贡献项可以是任意函数（条件/跨实体衍生，如火焰主宰数敌人燃烧）。
  - L2 结算区：PRE 订阅是**显式排序的修饰流水线**（priority 降序 + 注册序），加/乘/归零/veto 均可，**顺序敏感**（武者测试：加伤 priority 10 先于减免 priority 0）。不做固定乘区（PoE increased/more 式）——顺序敏感换表达力；若将来乘性修饰打架，退路是 priority 带约定（加性 ≥0、乘性 <0），不改内核。
  - L3 应用约定：技能算 amount/describe 显式读 getStat（同源不漂移）；效果只提供面板，不替技能结算。

## 下一步（当前阶段：玩法范围敲定）

按体系做原型验证（用 BattleDriver 写 headless 场景），每敲定一个就在 `SKILL_DESIGN_PRINCIPLES.md` 标注。建议顺序（机制压力从大到小）：

1. ~~**刀组合（卡序）**~~ ✅ 2026-07-31：`test/bladeCombo.test.js`（8 用例）——回旋斩牌库末抽牌、飞刀弃两侧、摘星手牌序格挡、断神斩 cooldownZones+手中衰败。zones 模型无需改动，全部机制现有原语可表达。
2. ~~**太极/混元系（计数触发）**~~ ✅ 2026-07-31：`test/chantCounting.test.js`（2 用例）——计数器放 skillRuntime（plain data，不用闭包藏状态），跨回合累积正常。
3. ~~**火灵脉（焚卡+自伤）**~~ ✅ 2026-07-31：`test/fireVein.test.js`（4 用例）——焚毁区不参与洗牌回库；燃烧自伤致死正确判负。
4. ~~**武者/武帝（格挡≥3 减免）**~~ ✅ 2026-07-31：`test/blockMastery.test.js`（4 用例）——能力走 PRE 订阅；priority 降序叠加（先加伤后减免）验证通过。
5. ~~**无量/拳师/拳王（行动力回复引擎）**~~ ✅ 2026-07-31：`test/apEngines.test.js`（5 用例）——抽牌计数跨指令累积+余数保留（skillRuntime.chantCount）；出牌计数直接读 history.turn.played（按回合自动重置），能力注册表可表达。
6. ~~**开刃/锻刀/真空斩（铸牌回库）**~~ ✅ 2026-07-31：`test/deckCraft.test.js`（3 用例）——TurnEnd POST once 订阅回牌库；**Core 新增 `AddCardInstruction`（rng 随机落点造牌）与 `MoveCardInstruction`**（cards.js），同种子落点可复现。
7. ~~**异步结算鲁棒性**~~ ✅ 2026-07-31：`test/asyncInput.test.js`（8 用例）——完美飞刀（焚两侧+牌库检索输入）、连续多段输入、敌方回合内输入（订阅触发的输入指令子类化挂后续）、挂起期间出牌/结束回合/重复应答均被拒、runToEnd 遇输入抛错、终局截断不产生残留请求。`createBattle` 已接线 `onWaitingCancelled` 清 pendingInput。
8. ~~**卡牌转化原语**~~ ✅ 2026-07-31：`TransformCardInstruction`（原地换绑 defId：uniqueID/zone/位置/power 延续，payload 白名单 toDefId 可被 PRE 改写）+ 生命周期元语 `enterBattle/leaveBattle`（skills/helpers.js；PreBattle 与 AddCard 均已收编走 enterBattle，顺带补上造牌不注册常驻订阅的缺口）。`test/transform.test.js`（8 用例）：订阅换绑双向、库内位置保持、充能重置、keepPower、PRE 升阶、点化输入集成、激活咏唱卡转化。
9. ~~**刀组合收尾 + 滞气**~~ ✅ 2026-07-31：`test/slashSeries.test.js`（6 用例）——语义对齐旧仓库：滞气=抽牌锁 debuff（PRE veto 一切抽牌 + 玩家回合结束递减）；藏锋（高伤+等量护盾+滞气）、超越（0费回AP+滞气锁当回合抽牌）、刀背打击（丢最右跳过自身）、回旋飞刀（多阶段：弃两侧→DrawCards→result.drawn 经 MoveCardInstruction 补进两侧空位）。
10. ~~**姿态系列（格挡效果形态）**~~ ✅ 2026-07-31：`test/posture.test.js`（3 用例）——格挡=buff 层数效果（受击减半+递减，**≠护盾池**，与旧仓库一致）；龟守/神龟姿态（回合开始格挡再生封顶 5 + 回合抽牌数 PRE 修正）。**Core 小扩**：`DrawCardsInstruction` 加 `reason` 字段（'turnStart'），供 filter 区分回合抽牌与技能抽牌。
11. ~~**假动作**~~ ✅ 2026-07-31：并入 `test/deckCraft.test.js`（抽3插3随机虚无，牌库守恒）。
12. ~~**机制框架压力批次**~~ ✅ 2026-07-31（+11 用例共 127 绿）。**Core 四处扩展**：①`DealDamageInstruction` 加 `tags` 标记位（群伤识别）+ minHp 地板（走 getStat 读轨）；②`UseSkillInstruction`/`ConsumeSkillResourcesInstruction` 加 `costOverride`（嵌套出牌费用豁免）；③`canUseSkill` 资源不足时进**能力裁决链**（ability def 可选 `canUseSkill(sctx, {manaOk, apOk})` 钩子，true 放行）；④`DrawCardsInstruction.reason`（上一批）。原型：`test/nestedPlay.test.js`（万变拳：嵌套出牌免费打任意手牌，资源/充能/计数/咏唱入槽全走标准管线）、`test/damageLifecycle.test.js`（反伤 POST 回击、闪避 PRE 归零、不灭 minHp 地板+塞西莉亚奇迹）、`test/elementalBurst.test.js`（爆裂术群伤、爆发单目标三倍、突破极限超费打出+clamp 兜底）。
13. ~~**面板修正系统（灼脉术）**~~ ✅ 2026-07-31：`test/panelModifiers.test.js`（3 用例，共 130 绿）。**Core 一处扩展**：`getStat(stat, view)` 加可选 view（battleState）参数，statModifiers 签名 `(stacks, unit, view)`，支持跨实体面板衍生（火焰主宰数敌人燃烧）。规则三层：L1 面板只加性走 getStat 读轨；L2 乘除/条件/veto 走 PRE 写轨；L3 技能算 amount/describe 显式读面板（同源不漂移）。灼脉术=纯内容（咏唱订阅"对敌上燃烧→获得集中"）。
14. ~~**换牌流程 + 咏唱槽扩容 + 锚定咏唱**~~ ✅ 2026-07-31：`test/swapCard.test.js`（5 用例，共 135 绿）。换牌对齐旧仓库语义（首 0 逐次 +1）：`SwapCardInstruction`（费用走资源指令子节点→弃牌→抽1 reason:'swap'→swapCount++）+ flow API `canSwapCard/playerSwapCard` + driver `swap()`；费用=min(swapBaseCost+swapCount, swapCostCap)，刀客/刀圣 onBattleStart 设 cap，归元秘术重置 swapCount。武神=onBattleStart chant.capacity+1。燃心决"无法撤下"=keyword 'anchored' 守卫 ManualStopChantInstruction。

**✅ 玩法缺口审计结论（2026-07-31）**：设计文档全量过一遍，**Core 结算已无缺口**。剩余未实现卡全部是已验证机制的内容级组合（节能=费用管线 PRE、发现三选一=input+AddCard、凝滞=veto AIAct、中毒=回合结束真伤递减、闪空灭=多段伤害、天一姿态=格挡层数供 attack 面板等）。**玩法范围敲定完成，可以开工 Bridge（§8.3）**。
15. ~~**Bridge 层首切（§8.3 五步全落地）**~~ ✅ 2026-07-31：`test/bridge.test.js`（7 用例，共 142 绿）。`src/bridge/`：events.js（EventNames 枚举+ANIM_TIMING 时序档，backendBus 状态/frontendBus 动画两条总线）、sequencer.js（animationSequencer 迁入，bus 注入，标签屏障+超时强杀+finish 回调协议原样）、presenter.js（Core presenter 21 个调用点全映射：战斗事件→动画指令（`_animId` 随载荷下发供 finish 回调）、生命周期→backendBus、requestInput→仲裁器、每次调用 markDirty）、projection.js（projectBattle：`_` 私有字段不外发、手牌/咏唱含 def 元数据+describe 文本、zones 覆盖层全量列表）、intents.js（操作+可用性查询，含 Core 新增 flow API `playerStopChant`）、interactionHandler.js（请求挂起/应答校验：kind/count/candidates/confirm）、index.js（createBridge 装配：双总线+队列+投影标脏拉取）。
16. Bridge 下一步（Stage 开工前可选补强）：①回合/AI 行动目前无 presenter 事件（Stage 需要 turn-start/ai-act 动画锚点时再补）；②BATTLE_LOG 只覆盖主要战斗事件，日志文案体系未建；③投影不含 history 统计（结算面板需要时再加）。④斩灭"必中"vs 闪避的 PRE priority 顺序是内容级调参；⑤滞气 veto 换牌抽卡（reason:'swap' 也是 DrawCards）符合"无法抽牌"语义，未单独测试。随后进入 **§8.4 Stage 基座**：StageManager+相机约定 → RichTextEngine（parser/layout/texture/hit map 铁律）→ CardObject → LayoutEngine → StageAnimator → Picker。

**⚠ 语义分歧待决策**：旧仓库的**格挡**是效果层数（受击减半+递减），与护盾池是两个机制；早前摘星手/武者/武帝原型拿护盾池当了替身。posture 批次已按旧语义落地效果形态，两者在 Core 都可表达，但"武者：格挡3层以上减免75%"的准确挂载点（格挡层数 vs 护盾值）需要敲定后统一。

17. ~~**Stage 基座首切（§8.4 六个模块）**~~ ✅ 2026-07-31（+9 测试文件 +45 用例，共 **32 文件 187 绿**）。`src/stage/`：
   - `richtext/parser.js`：markup 解析（`/颜色{}` `/effect{}` `/named{}` `/skill{±N}`），逻辑等价旧 ColoredText.vue 四正则；重叠 match 丢弃策略（比旧实现更稳）。
   - `richtext/layout.js`：token→逐字形放置+hit map，**measure 注入**（node 可全测）；named/skill 产 hitRegions，跨行每行一段。
   - `richtext/texture.js`：离屏 canvas→CanvasTexture，scale 倍率烘焙+mipmap；**纹理与 hitRegions 成对替换**（§4.6 铁律）；canvas 工厂/drawIcon 注入。
   - `StageManager.js`：正交相机，**屏幕高=100 世界单位、原点居中、y 向上**，resize 只改视锥；`screenToWorld`；场景切换 onEnter/onExit；renderer 注入（node 无 WebGL）。Z_LAYERS 分层常量。
   - `objects/CardObject.js`：Group+face plane；`setCard` 重烘+旧纹理 dispose；`hitTestUV`（uv→局部坐标→hit map，**three 交点 uv 是 Vector2(x,y)，Picker 侧适配 u=x,v=y**）；setVisualState 占位（shader 后补）。
   - `layout/LayoutEngine.js`：SkillsHand.vue 扇形算法移植（间隙压缩/hover 撑开衰减/整体居中），像素值按屏高 1000px≈100wu 换算；`updateAnchors(containerKey, Map)` 契约签名保留 + 命名锚点（deck/center…）。
   - `animator/StageAnimator.js`：旧 animator.js 状态机 three 版（idle|tracking|animating|dragging，完成自动回落 idle，切换 kill tween），gsap timeline 默认实现，**tween 工厂注入**（单测用同步/手动假 tween）。
   - `picker/Picker.js`：真 raycast（node 可测）；优先级 token 热区>整卡>按钮>背景；hover 事件流 TOOLTIP_SHOW/MOVE/HIDE + CARD_HOVER/LEAVE（事件名已入 bridge/events.js，Shell 消费；接哪条总线装配时定）。**坑：相机不在场景图内，pick 前须 `camera.updateMatrixWorld()`**。
   - `test/stageEventLoop.test.js`：fake Stage 闭环契约——ANIM_* 严格串行下发（前一条 finish 前无后一条）、finish 驱动队列推进、animator 状态 animating→idle。这就是 BattleStage 视觉导演要遵守的接线模式。
   - **注意**：起手抽牌只发一条合并的 `anim:card-drawn`（不是每张一条）。

18. ~~**BattleStage 场景组装 + 调试页**~~ ✅ 2026-07-31（+1 测试文件 +6 用例，共 **33 文件 193 绿** + playwright 真实浏览器冒烟通过）。
   - `src/stage/objects/UnitObject.js`：占位 quad（side 配色）+ 文本 label（RichTextEngine 烘焙，签名不变不重烘；**plane 尺寸=烘焙像素/10，全项目统一 10px=1 世界单位**）。
   - `src/stage/stages/BattleStage.js`：reconcile（STATE_DIRTY→拉投影→建销 CardObject/UnitObject/主按钮，投影是唯一事实源）+ 视觉导演（ANIM_*→脉冲/死亡缩放→回 finish，契约同 stageEventLoop）+ 输入（拖拽过 y=-20 出牌线=打出、主按钮=结束回合/确认、结算期点选候选卡 respond）。布局：手牌 y=-38 扇形、咏唱 (-12,-13)、玩家 (-30,5)、瑞米 (-13,5)、敌人 x=14+18i y=10、主按钮 (44,-12)、牌库锚 (-46,-38)。
   - `src/debug/main.js` + `index.html`：调试页（牌组 punch/guard/inflame/focusChant + slime+pyro + remi，DOM 覆盖层：计数/日志/tooltip/终局），`window.__debug` 钩子。`npm run dev`（端口 5177）可玩。
   - playwright 冒烟（1920x1080）：拖拽出牌掉血进弃牌堆✓、结束回合敌人行动+回合抽牌✓、hover 扇形撑开✓、16:9 布局✓。
   - **修掉的坑**：①Bridge 投影缓存穿透——Core 收尾 zone 迁移（UseSkill stage 2 的 moveCard）不经 presenter、不标脏；修复=index.js 在**意图/应答调用返回后强制 markDirty**（意图是所有变更的统一入口，比给 Core 补 presenter 调用更稳）。②LayoutEngine 旧 z=10+i+1000 是 DOM z-index 思维，three 相机在 z=100，悬浮卡飞到相机后面被剔除——改为 z∈[10,40]。③CardObject hitTestUV 改用烘焙布局尺寸（与牌面世界尺寸解耦）。④`Player` 默认 name='玩家'（日志"对 造成"空名）。
   - **显示假设：固定 16:9（1920x1080，世界宽≈177.8），不适配其它比例**（已写入 StageManager 头注释）。
   - 已知占位（后续补）：打出/离场动画=立即消失、/effect 图标=白框、按钮无底板、UnitObject 无立绘、拖拽中无目标高亮。

19. ~~**牌面表现 + tooltip 联动修复**~~ ✅ 2026-07-31（+2 测试文件 +4 用例，共 **34 文件 197 绿**）。
   - **tooltip 不联动的根因**：`/effect{}` token 根本没产 hitRegion（layout.js 只给 named/skill 产了）。修复：effect 图标也产 `{type:'effect', payload:{name}}` 热区；`isInteractiveToken` 同步。调试页 tooltipContent 经 `allEffects()/allSkills()` 按显示名反查定义，tooltip 展示图标+名称+描述（skill 展示费用）。markup 里写的是显示名而非 id，按 `def.name` 匹配。
   - `src/stage/richtext/cardFace.js`：牌面烘焙器（200x270 固定布局盒）——圆角底板+品阶色边框（D灰/C蓝/B紫/A金/S红）、名称+威力、右上费用双徽章（蓝 mana/绿 AP）、正文区内衬+富文本、底部关键词/咏唱/充能栏。正文热区加 BODY_OFFSET(12,56) 偏移后随纹理成对返回。texture.js 抽出 `drawPlacements`（带偏移）供复合烘焙复用；默认 drawIcon 从白框改为圆形徽章（effect 橙/skill 蓝）。
   - `projection.projectCardFull` 补 `tier` 字段（压平定义数据，Stage 不碰注册表的约定不变）。
   - 手牌容器 y=-38→-35（27 高的牌底边曾越出 -50 屏幕下界被裁）。
   - playwright 验证：牌面设计渲染 ✓、hover 燃烧图标弹 tooltip（图标+描述）✓。

20. ~~**牌库/坟墓图标 + 卡流动动画**~~ ✅ 2026-07-31（+1 测试文件 +6 用例，共 **35 文件 203 绿** + playwright 验证）。
   - **视觉模型确立（替代老版事件驱动 DOM 的混乱）**：**状态差分驱动视觉，ANIM 事件只做强调与节拍**。新卡从牌库图标处生成、跟踪飞入手牌；离场卡按投影反查落点（弃→坟墓图标/回库→牌库图标/焚→原地溶解占位）播飞行后销毁——不依赖 ANIM 事件载荷完备性（UseSkill 收尾的静默 zone 迁移也正确播离场）。ANIM_CARD_DRAWN/ADDED→脉冲牌库、DISCARDED/SWAPPED→脉冲坟墓、BURNT→直接 finish。
   - `src/stage/objects/ZonePileObject.js`：卡背图标（斜纹+区域名+大计数，setCount 变化才重烘）。位置：牌库 (62,-35)、坟墓 (62,-13)，手牌右侧纵列。点击开**区域查看器**（全屏暗化 bg z=80 + 卡网格 z=81 只读，任意点击/STATE_DIRTY 关闭；打开期间屏蔽拖拽）。
   - 投影 zones.deck/discard/burnt 升级为 projectCardFull（查看器烘焙牌面需要定义数据）。
   - playwright 验证：图标计数 ✓、出牌飞坟墓（discard 0→1、 slime 掉血）✓、点牌库开查看器（4 卡网格+暗化）✓。
   - 离场 dissolve 仍是缩放占位，焚毁 shader 后补。

21. ~~**演出加强 + 粒子雏形 + 图标化区域图标**~~ ✅ 2026-07-31（+2 测试文件 +7 用例，共 **37 文件 210 绿** + playwright 验证）。
   - `src/stage/particles/ParticleSystem.js`：单 THREE.Points + 对象池（max 512），spawn(位置,{count/color/speed/ttl/gravity})→update(dt) 推进衰减；AdditiveBlending 下黑色=不可见，死粒子颜色归零还池。StageManager 新增 `onTick(fn)` 帧回调（THREE.Clock，单帧 dt 封顶 100ms 防掉帧跳变），BattleStage 接线驱动粒子。
   - **发动展示**：ANIM_SKILL_USED → 幽灵卡（从投影反查牌面视图）中央弹出 180ms → **停留 380ms**（gsapTween 新增 delayMs 纯延迟 tween，停留节拍）→ 收走销毁，队列严格串行保证节奏。
   - **受伤演出**：ANIM_DAMAGE → 目标闪红 + 红橙粒子爆发(16) + 短促击退震动（x+1.8 80ms → 归位 220ms）；HEAL/SHIELD/EFFECT 各配绿/蓝/黄粒子；死亡灰色爆发(22)+缩小收殓。
   - **🔴 潜伏大坑修掉**：`_syncUnits` 创建单位时**从未 animator.register**，所有单位动画 animate() 静默返回 null、finish 链断、动画队列停摆（真实浏览器里靠 ANIM_TIMING 超时强杀才没卡死，之前战斗演出全靠超时兜底在跑！）。修复：单位注册/注销补上；另给 StageAnimator.animate 加保险——未注册 id 立即回调 onComplete 而不是静默丢失。
   - 牌库/坟墓图标缩小为 icon 型（16x20→10x12 世界单位，斜纹卡背+小字）。
   - playwright 验证：幽灵卡中央停留（截图抓到）✓、出牌小卡飞坟墓 ✓、粒子 activeCount>0 且玩家盾+5 ✓、图标缩小 ✓。

22. ~~**围绕卡牌的战斗演出打磨（高完成度批次）**~~ ✅ 2026-07-31（+7 用例，共 **37 文件 217 绿** + playwright 验证）。
   - **🔴 打出-发动-回库时序 bug（用户点名）**：离场 tween 在 reconcile 时立即播，与发动幽灵卡并行抢戏。修复=**离场暂存制**：`_pendingDepartures`（隐身挂起），导演在①下一 ANIM 节拍开始②队列排空（finish 后 pendingCount===0）③reconcile 时队列空闲 三个时机 `_flushDepartures()` 放飞；放飞前有同 uniqueID 重生身份检查（`_flushDepartures` 内 animator.getObject(id)===object 否则直接销毁）。配套把 `ANIM_TIMING[ANIM_SKILL_USED]` 400→**800**（原先 400ms 超时强杀早于幽灵卡实际 720ms 演完，节拍形同虚设）。
   - **🔴 潜伏坑 2 号**：ANIM_CHANT_STARTED 的通用 scale 脉冲 animate() 会 **kill 掉入槽的跟踪飞行**，把咏唱卡晾在半路（真实浏览器复现：卡停在 (-20,-10) 而非左列）。修复：CHANT_STARTED 只打节拍不脉冲（激活表达交给边缘流光）；通用脉冲完成后若目标还在桌上 → 重回 enterTracking。
   - **可发动性着色**：`_layoutAndTrack` 视觉态优先级：结算期候选（highlighted/disabled）> 手牌 canPlayCard（不可发动淡灰白 0xb8b8b8）> normal。
   - **牌面视觉升级**（cardFace.js 重写 drawFrame/drawHeader）：系列主题色 `TYPE_COLORS`（normal灰/fire红/wood绿/water蓝…，底板与内衬按品阶浓度 mixHex 混入主题色+名称下主题分隔线+footer 主题色）；等阶 style：边框宽度 D2.5→S6.5、B 及以上加细内描边、左上角菱形品阶徽章（字母）；投影 `projectCardFull` 补 `type/series/image`。
   - **卡面图案**：`src/stage/art/cardArtCache.js`——`import.meta.glob('../../assets/cards/*.png')` 静态收集 URL；解析规则 def.image（不含扩展名）→ 兜底 `${type}-${tierIndex}.png`（D=0..S=4，素材 fire-1..4/wood-0..4 等）→ 无图。get() 同步查询未命中即异步加载，onLoad 回调 Stage `_rebakeCardFaces()` 全量重烘。烘焙端 `bakeCardFace({art})`：图区 (12,46,176x88) cover 裁切，**有图时正文区顶从 56 下移到 142**（热区同步偏移）。
   - **咏唱卡左列**：LayoutEngine 新增 `layoutColumn`（topY 向下纵列，z 独立低区间 4+i*0.5 低于手牌 10+）；registerContainer 改为原样存配置（曾解构丢字段致 topY undefined→anchor NaN）。咏唱槽 (-74, 自 32 向下，gap 3)。**激活边缘流光**：`CardObject.setActiveGlow/updateGlow(dt)`——Additive 小光点沿牌边缘矩形周长路径循环（≈2.9s/圈）+ 呼吸脉动，BattleStage tick 驱动，幂等随 reconcile 开关。
   - **冷却/powerUp non-blocking 脉冲**：`CardObject.ensureOverlay()`（惰性 Additive 发光片）+ `BattleStage._pulseCard(id,color)`——FX tween 走注入工厂（StageAnimator 导出 gsapTween），**不进注册表、不占队列**。ANIM_COOLDOWN_TICK→绿脉冲+立即 finish（反向/强冷却差异化等待 presenter 带 delta）；power 提升（reconcile 比 prevPower）→金脉冲。
   - **拖动指定目标（本批唯一 Core 契约改动）**：`playerUseSkill(battle, uniqueID, targetUniqueID)` → `UseSkillInstruction.targetUniqueID` → stage 1 **结算时**白名单解析（全场存活单位，死亡/找不到落 null）→ `ActivateSkillInstruction.target` → `sctx.target`。技能侧约定：**敌单体牌用 `enemyTarget(sctx)`**（target 是存活敌人才采用，否则 firstAliveEnemy）。intents.playCard 透传。Stage 侧：拖拽中 `picker.pick(x,y,{kinds:['unit'],excludeIds:[dragId]})`（Picker.pick 新增 filter 参数）掠过存活敌人 → `UnitObject.setHighlight`（金色衬环）；松手在敌人身上=指定目标打出（不再要求过出牌线）。尸体/友方不算目标。
   - 牌库/坟墓图标 x: 62→**76**（6 张手牌扇区最大 ±65，曾压住牌库图标）。
   - playwright 验证：拖冲拳到燃焰术士（金环高亮截图✓、log"对 燃焰术士 造成 6 点伤害"、史莱姆无伤）✓、咏唱入左列+流光光点绕边移动（两帧位置不同）✓、点火卡 fire-1.png 卡图+正文下移 ✓、AP 耗尽手牌淡灰白 ✓、幽灵卡与离场飞行时序 ✓。

23. ~~**全局唯一卡牌模型（废除幽灵卡）+ scale 衔接修复**~~ ✅ 2026-07-31（用例数不变，**37 文件 217 绿** + playwright 验证）。
   - **设计转向（用户提出，确认成立）**：幽灵卡违背"投影是唯一事实源"——一张卡任何时候应恰有一个视觉实体。替身制造了全部衔接缝（真卡隐身/替身 0.5 小尺寸弹出/替身收走真卡原位重现）。**新模型：发动展示用卡本体**——打出的卡从当前位置（手牌/松手点/离场暂存位）飞中央放大 1.15 停留，随后**同一对象**衔接离场飞行（坟墓/回库/焚毁溶解）或飞回咏唱列锚点。发动展示天然阻塞回库（回库=展示的最后一段），无需任何 handoff 补丁。
   - 实现：`_skillGhost/_findCardView` 删除 → `_skillDisplay/_releaseDisplay/_flyOut`；`_parkDeparture` **不再隐身**（唯一实体原地多停一拍，比"消失又出现"自然）；`_flushDepartures({skipId})` 跳过本节拍主角与展示中的卡（连打时 B 卡的离场不会被 A 的节拍顶掉）；`_inFlight`  Map 管离场飞行中对象，同 uniqueID 重生（弃牌堆捞回）时清尸防泄漏；展示载体查找 `_cards ?? _pendingDepartures`（队列繁忙时卡已暂存也能展示）。
   - **scale 硬跳清零**：新抽卡从牌库图标 scale 0.5 长开（曾全尺寸凭空出现）；通用脉冲/图标脉冲回程改为补间（卡片 enterTracking 回锚点含悬浮 scale / 单位补间回 1.0），废除两处 `scale.set(1,1,1)` 硬切。
   - playwright 验证：展示中段本体在 (0,-2) scale 1.15（点火卡带卡图居中）✓、展示后同对象销毁进坟墓 ✓、抽牌飞行中段 scale 0.86 长开→落定全 1 ✓。
   - **补：咏唱卡点按取消**（曾完全不可点——`handlePointerDown` 只对手牌 `canPlayCard` 起拖，咏唱卡无点击路径）：down 在咏唱卡（`canStopChant`）记 `_pressChant` 候选，up 落在同卡（整卡或卡面 token）→ `intents.stopChant`；点到别处不触发。anchored 咏唱点按走 `chantStopFailed` → 通用脉冲反馈（回程平滑修复已覆盖此路径）。+1 用例（共 **218 绿**）。
   - **补：玩家资源点状显示**（`src/stage/objects/ResourcePipsObject.js`）：数字文本（`AP 1/3` / `魏启 2/3`，签名不变不重烘）+ 圆点横排（AP 黄 0xf0c040 / 魏启蓝 0x4a8fe8，耗尽点灰 0x555555 常驻不消失，数量只随上限变），文本+点排整体居中。BattleStage 挂在手牌栏上方（AP y=-14.5、魏启 y=-18，z=6），reconcile 时按投影同步。+1 用例（共 **219 绿**），playwright 截图验证（AP 1/3 一黄两灰、魏启 2/3 两蓝一灰）。
24. ~~**资源点交互效果（高亮即将消耗 + 消耗切换动效 + 平滑过渡）**~~ ✅ 2026-08-01（+1 用例，共 **37 文件 220 绿** + playwright 验证）。
   - **ResourcePipsObject 帧驱动化**：点颜色不再 setValue 直写，改为 `update(dt)`（挂 StageManager tick）每帧向目标色 lerp、足够接近（<1/255）吸附精确值——消耗变色、高亮启止全部由此平滑过渡，测试可断言收敛色。
   - `setPending(n)`：高亮"即将消耗"的 n 个点（充盈点列末尾 n 个），目标色在资源色与白色之间正弦脉动（相位错开成波纹）；脉动目标不停移动故永不吸附、持续渐变。BattleStage `_updatePendingPips()`：悬浮**手牌**按其 `cost`（`{mana, actionPoint}`）点亮 AP/魏启点（咏唱卡费用已付不高亮），hover 切换与 reconcile（卡离场/数值变）时重算。
   - 消耗切换动效：充盈状态翻转时点 pop=1 → scale 1.35 短促弹跳随帧衰减回 1（non-blocking）。
   - playwright 验证：真实鼠标悬浮冲拳（1AP）→ 末尾 AP 点脉动至近白（faeeda），魏启不动；打出后该点 0.5s 内白→灰平滑收敛精确 0x555555、scale 1.31→1。**注意**：页面真实 pointermove 会覆盖合成 bus 事件，验证须用 `page.mouse.move` 真悬浮。
   - 既有断言适配：消耗变灰改为渐变后，旧"立即变灰"断言改为 `settlePips()`（推进 update 至收敛）后断言。
25. ~~**两套状态 + state-sync 动画指令（老版 animationSequencer 精髓移植）+ 离场阻塞节拍**~~ ✅ 2026-08-01（+3 用例，共 **37 文件 223 绿** + playwright 埋点验证）。**本批是架构级重构，推翻了第 19-23 条的"状态差分即时驱动视觉"模型。**
   - **问题**：①打出卡牌后离场飞行与敌人受伤动画同时开始（应先发动→受伤→离场）；②敌人先扣血文本再播受伤动画；③卡牌没飞进坟堆数字就+1；④ANIM_TIMING 档位偏紧，复杂结算可能被超时强杀。
   - **老版设计（master:src/data/animationInstructionHelpers.js，已通读）**：`backendGameState`/`displayGameState` 两套状态；`enqueueState()` 把状态同步做成**专有动画指令**（tags:['state']、waitTags:['all']、快照入队时 capture、apply 即 start）；脏状态在任何新动画指令入队前自动补一次 sync；sequencer 是 command queue（多指令并发 running，tags/waitTags 定阻塞），不是串行管道。
   - **新实现**：
     - `ANIM_STATE_SYNC` 事件（`src/bridge/events.js`）。presenter 每个方法按**方向**决定 sync 与动画节拍的相对位置：效果类（伤害/治疗/护盾/资源/特效/死亡）→ 先动画后 sync（演完再变数字）；入场类（抽牌/造牌/转化/咏唱入槽/技能播报）→ 先 sync 后动画（先转移再播）；离场类（弃/焚/迁移/换牌/停咏唱）→ 先离场飞行后 sync（飞进坟堆数字才+1）；requestInput 前先 sync（玩家决策时显示状态必须最新）。
     - **快照在入队时捕获**（关键正确性）：内核同步结算完毕，若 start 时才拉快照，首个 sync 就会剧透终态（卡牌没飞坟堆已+1）。入队捕获 = 一条结算链渐进揭示。
     - bridge 兜底：`syncIfIdle()`（markDirty / ANIMATION_INSTRUCTION_FINISHED / intents / respond / setTimeout 五处触发，带递归守卫）——队列排空时显示状态必定追上后端。
     - core `UseSkillInstruction` 收尾补 `cardBurnt`/`cardMoved` 播报（此前静默 moveCard，离场无节拍）。
     - BattleStage：`STATE_DIRTY` 不再驱动场景；`_snapshot` 只在 sync 节拍应用（`_applySnapshot`→reconcile）；`_pendingDepartures/_flushDepartures/_parkDeparture` 全删——离场飞行由 `ANIM_CARD_*` 节拍**阻塞播放**（`_departureBeat`，落点取节拍载荷 toZone 而非投影）；发动展示后卡回手牌锚点，等自己的离场节拍。
     - ANIM_TIMING 全部放宽为 ≥2000ms 的保险丝（注释明示：超时不参与调度，节拍衔接一律以 finish 为准）；presenter 未列名事件兜底 2500。
   - playwright 埋点验证（页面内包 hook 记 performance.now）：冲拳 → 敌人 flash 后 **306ms** HP 文本才变（伤害动画播完才扣血数字）；离场飞行开始后 **300ms**（飞行播完）坟堆计数才 +1。
   - 测试适配要点：假 Stage 会把 sync 节拍也当动画（stageEventLoop）；bridge 严格串行测试改为"非 sync 节拍次序 + 方向性断言（damage/cardMoved 后必跟 sync、drawn 前必有 sync）"；ANIM_TIMING 档位契约测试（≥2000）锁定防收紧。
   - **补：展示卡不回手牌（`_heldCards`）**：发动展示收尾分两路——`sequencer.findPending`（新增只读查询）发现该卡已有离场节拍在排队（正常打出/焚毁）→ **停留展示位**等节拍来收（`_layoutAndTrack` 对 held 卡跳过 enterTracking，离场节拍取出时清除）；否则（咏唱入槽 / 结算期输入挂起时离场节拍尚未产生）→ 回锚点跟踪。修复"展示完飞回手牌、伤害后被离场节拍又拉进坟堆"的折返跑。playwright 采样验证：伤害节拍期间卡稳定在展示位 (0,-2)。回归断言：展示记录与离场飞行记录之间无 hand 锚点 tween。
26. ~~**textured/text 粒子 + 伤害数字迸射**~~ ✅ 2026-08-01（+3 用例，共 **37 文件 226 绿** + playwright 截图验证）。
   - **ParticleSystem 双池化**（`src/stage/particles/ParticleSystem.js`）：保留 Points 点粒子池，新增 **Sprite 精灵池**（textured/text 粒子，独立材质各带纹理与透明度，上限 64）。物理：初速度 + `gravity`（g，世界单位/秒²，y 向上下坠为负）+ `drag` 阻力（`v *= (1-drag)^dt`）；表现：透明度线性/三角（fadeIn）衰减 + `scalePop` 出生弹跳（0.25s 回落）。`spawnSprite`（任意纹理）/ `spawnText`（文本经注入 bakeText 烘焙一次性纹理，死亡即销毁）；浏览器默认走 RichTextEngine，**行高随字号（fontSize×1.3）防大字号裁剪**。
   - **richtext fontWeight 透传**：DEFAULT_TEXT_STYLE 增 `fontWeight`（默认 normal），layout placement → drawPlacements/measurer 的 `ctx.font` 全链路带上（伤害数字加粗）。
   - **伤害交互**（老版 HurtAnimationWrapper 配方演进）：`_damageHit` 按载荷迸射文本粒子——HP 伤害 `-{dealt}` 红色加粗、字号随伤害缩放（34+2.4/点，cap 96px）、向上迸射受重力下坠、出生弹跳；护盾吸收独立 `-{absorbed}` 灰蓝小字偏移开（全挡时只有它）；治疗 `+{healed}` 绿色无重力上飘（drag 减速）。
   - playwright 验证：点火 `-2`、燃焰术士打玩家 `-5` 均在闪红单位上方迸射，加粗完整可读；曾踩坑——大字号默认行高裁剪半个字（行高修复）、AP 耗尽时 playCard 静默失败导致"无粒子"误判。
27. ~~**战斗按钮改版 + 换卡按钮实装**~~ ✅ 2026-08-01（+2 用例，共 **37 文件 228 绿** + playwright 截图验证）。
   - **风格化按钮烘焙**（`src/stage/richtext/buttonFace.js`）：圆角矩形底（纵向渐变）+ 描边 + 主/副标签双行文本，三态主题（enabled 蓝钢金边 / active 翠绿高亮 / disabled 深灰），与 bakeCardFace 同契约（10px/wu 局部坐标、scale 2、mipmap 约定一致）；仅浏览器可用，单测走注入 bakeLabel 透传。
   - **BattleStage 双按钮纵列**（`BUTTON_POSITIONS` 导出：main (46,-8) / swap (46,-16)，15x6 世界单位，较旧 18x9 明显收小；`_buttons` + `_buttonSigs` 签名去抖重烘）。主按钮 = 结束回合，结算期退化为确认/选择计数（语义不变）；`_syncButtons` 统一驱动。
   - **换卡模式**（`_swapMode`）：点换卡按钮进入（可用性以按钮面 `cardData.enabled` 为准：自由行动窗 + 手牌非空 + AP ≥ `proj.swapCost`），手牌按 `canSwapCard` 高亮/压灰（视觉态优先级：结算期选卡 > 换卡模式 > 可发动性），点一张手牌换出（弃 1 抽 1）后自动退出；再点按钮取消。模式下 pointerdown 拦截拖拽（点按 ≠ 起拖）；行动窗关闭（结算输入出现/回合结束）由 `_syncButtons` 自动退模式。按钮副标签实时显示 `⚡{swapCost}`（0 起逐次 +1）。
   - 测试要点：换卡用例牌库须 > 初始抽牌数（4 张全抽上手时换牌弃牌会立刻被 DrawCards 洗回牌库，discard 计数不变——fixture 用 6 张）。
28. ~~**舞台设计草案 + seedream 素材管线 + 首批素材**~~ ✅ 2026-08-01（纯文档/工具/资产，无测试变更）。
   - **设计文档 `quest_prompts/STAGE_DESIGN.md`**：正交相机不动、纵深用**战线轴**伪造（槽位 t → position/scale/z 全部 lerp，近大远小、近处压远处）；分层 z：backdrop(-10) < 地面椭圆阴影 < 立牌(按 t 排) < 粒子(20) < UI(30+)；UnitObject 立牌化结构（shadow/standee/hpBar/fxAnchor）+ 极简状态机（idle 呼吸 / attack lunge / hurt 抖动红闪 / dead 倒地淡出，仿射只打 standee）；SceneDefinition 注册表（`src/stage/scenes/`，战斗→场景映射先固定 dungeon）。
   - **素材管线 `tools/seedream/`**：`gen.py`（manifest 驱动，refImages 本地图→base64 i2i，ARK_API_KEY 只走环境变量、**永不入文件**，watermark=False 可用，单资产失败不中断批次）；`key.py`（边缘洪水抠图：四边估背景色 → BFS 只去连通背景，内部白色不误抠，羽化带+自动裁边）；`manifest.json`（风格关键词锁定：粗糙笔触涂鸦水彩+Q版二头身+粗犷勾线）；venv 在 `tools/seedream/.venv/`（已 gitignore，顶层 `out/` 规则顺带覆盖产物目录）。
   - **首批 5 资产 1K 试水全部一次过**（约 60-77s/张）：bg_dungeon（1424x800 地牢长廊，拱门/火把/吊灯/尽头门/中左空地全中）、unit_player（灰甲蓝披风小骑士）、unit_remi（i2i 自旧 remi.png，白色大耳猫形小精灵全身像）、unit_slime（i2i 自旧 slime.png，黑团白眼）、unit_warlock（红袍火焰法杖凶萌术士）。成品已拷入 `src/assets/stage/`（bg 为 jpg——API 实际返回 JPEG 字节流，命名修正）。
   - **抠图教训**：默认 tol=32 会穿透白色角色（Remi 白毛与 253 白底色差 <32，BFS 从浅色描边缺口灌入内部）→ 白色角色用 `--tol 12 --feather 1.2`（背景近纯白，小阈值足够，深色轮廓线正好挡水）。
   - 旧资产参考图已拷入 `tools/seedream/ref/{remi,slime}.png`（出自 master:src/assets/）。
   - **补（角色概念图 i2i 重生成）**：用户补了主角/瑞米的手绘概念稿（存 `tools/seedream/ref/player_concept.png` / `remi_concept.png`），两张重生成均一次过并替换成品——unit_player：无装饰全身板甲倒三角壮汉、Y形面甲缝、红围巾、长剑+铁锤；unit_remi：正面坐姿、尾巴比身体还大的蓬松尾、豆豆眉。i2i 对概念稿的形象还原度极高，**后续新角色流程 = 手绘概念稿 → ref/ → manifest 加条目 → gen → key**。
   - 粘贴图定位技巧：kimi CLI 粘贴的图片不落盘，本次从 `~/Downloads`（Krita 导出命名 `未命名 *_时间戳.png`）找到；bash 通配符拷贝规避中文文件名编码问题。
29. ~~**2.5D 舞台搭建（背视立牌 + 战线轴 + 背景画 + HP 条）**~~ ✅ 2026-08-01（228 绿 + playwright 截图验证两轮）。
   - **视角约定**（用户定）：友军背对屏幕、敌军正视。补生成 `unit_player_back`/`unit_remi_back`（i2i 概念稿，一次过）；瑞米背视图按用户反馈改"粗糙幼稚铅笔草稿"风（去精致萌系 AI 感）。成品命名约定：`unit_xxx.png`=舞台用图（友军背视/敌军正视），`unit_xxx_front.png`=正视备用。`ref/remi.png`（旧头像）已被用户删除，不再使用。
   - **`src/stage/art/unitArt.js`**：defId/side→立牌映射（player 固定，remi/slime/pyro 查表）+ UnitArtCache（CardArtCache 同模式异步加载+onLoad 补挂）+ 体型系数表（player 0.92/remi 0.62/slime 0.6/pyro 0.85，基准 26wu）。
   - **`src/stage/scenes/`**：SceneDefinition（battleLine 战线轴 near/far/scale/z + lane 横排槽位表）+ `slotTransform()`（t → position/scale/z lerp，lane 沿法向偏移）+ BACKDROP_URLS。战斗→场景先固定 dungeon。
   - **UnitObject 立牌化重写**：shadow（贴地椭圆）+ standee 子组（底部锚定 plane，呼吸 scaleY 0.02 sin 相位随机，仿射只打它）+ hpBar（底槽+左锚定填充条+`20/60 盾5`数字文本，对齐手绘稿）+ fxAnchor（效果图标留位）。setArt 按图纵横比重排；flash=材质染色（有图白/无图 side 色）；高亮改地面金环站台（加色混合，贴合 2.5D 地面语言）。
   - **BattleStage**：backdrop 整屏 plane（z=-10，浏览器异步挂图，node 深色兜底）；`_syncUnits` 走 slotTransform + `_baseScale`（通用脉冲围绕 baseScale 起伏，死亡单位不重放 scale 防 reconcile 踩回收殓补间）；立牌 onLoad 统一补挂（`_applyUnitArt`）；tick 驱动全单位呼吸。
   - **调试记录**：①瑞米初版槽位（t:0.13 lane:1.1）被主角立牌完全遮挡（近处 z 高压远处）→ 改放主角右前方（t:0.22 lane:-0.9）；②拖牌测试硬编码旧槽位坐标 (32,10) → 改读对象实际位置 +4y（组原点在脚底锚点）；③死亡后 reconcile 把 scale 重放回 baseScale 踩掉收殓 → isDead 跳过 scale 重放。
30. ~~**小 FOV 透视相机 + 程序化低多边形 3D 场景**~~ ✅ 2026-08-01（38 文件 233 绿 + playwright 截图验证两轮）。**推翻两个方案：正交相机（用户要可感纵深）与生图背景（用户：程序化场景费效比更高）。**
   - **透视相机**（StageManager）：PerspectiveCamera fov=24°，距离由 worldHeight 反算（z=0 平面与旧正交约定**完全一致**，布局不动）；`screenToWorld(px,py,planeZ)` 改射线与 z 平面求交（任意相机通用），新增 `worldToScreen` 逆映射；Picker 改为 ndc 直算（不再绕道 screenToWorld）；拖拽出牌映射到 z=30 同深平面防视差。战线轴 battleLine 3D 化（near z=+16 / far z=-28），nearScale/farScale 只留 1.05/0.9 风格化夸张。
   - **测试基建教训**：`toScreen` 线性近似 `(wx+50)*10` 只在 z=0 成立——全部测试 helper 改走 `sm.worldToScreen(wx,wy,wz)` 并传对象真实 z（picker.test.js 对象全在 z=0，数学等价未改）。
   - **程序化地牢**（`src/stage/scenes/dungeon3D.js`，纯几何零资产）：**坡面地板**（小 FOV 下水平面侧视成线，坡面既铺地面又让站位带全落投影内）、背景墙+木门、六棱柱两列三排、火把×4（铁架+火焰锥+点光+粒子锚）、吊灯（铁环+5 蜡烛焰+中央点光，填补上半空间）、碎石；three 物理光度学点光（2600/1800 candela）双频 sin 闪烁。SceneDefinition 改 `build3D()` 工厂契约。
   - **氛围**：THREE.Fog(245,330) 远景没入黑暗但保留墙/门剪影（立牌/HP条/粒子全 fog:false 豁免，否则敌人被雾吞 47%）；火焰粒子自火把锚点上浮（gravity>0）+ 浮尘；**立牌光照交互用假采样**（`sampleStandeeTint`：冷底+火把/吊灯暖光 XY 衰减与闪烁同源+纵深压暗）而非实光照—— Lambert 在 4 顶点平面上同样是平涂，假采样可精确控制且闪红窗口（`_flashT`）不覆盖。
   - **调试记录**：①初版雾（238,292）把背景墙吞成全黑、上半空间全空 → 放宽（245,330）+ 加吊灯；②吊灯后来加入染色采样（初版只采火把，灯下单位偏冷）。
   - 弃用：`bg_dungeon.jpg` 生图背景（成品仍留在 src/assets/stage/，未引用）；BACKDROP_URLS 已删。
31. ~~**斜视相机 + 水平地板（透视一致性根治）**~~ ✅ 2026-08-01（233 绿 + playwright 截图/交互验证三轮）。
   - **用户观察**：柱子顶端像"从下往上看"，但整个场景像"从上往下看"——问：一致 world space 怎么会透视不一致？**答：投影矩阵没坏，是眼高数据问题**。旧相机 y=0（轴对齐）正好夹在场景中部：柱帽（y>0）在眼上→合法露底面（读作仰视），坡面地板法线朝镜头→合法露顶面（读作俯视）。单投影数学一致，但人眼对建筑内容的预期是"水平面都在眼高同侧"，所以读成两个视角拼接。
   - **相机**：`CAMERA_HEIGHT=30`、`CAMERA_LOOK_AT_Y=-15`，position (0,30,235) + lookAt——**眼高高于场内一切水平面**（地板 -30、柱帽顶 ≈+20），透视方向全场一致；lookAt 压低给手牌栏留构图。z=0 平面只"近似"旧约定（屏幕中心 = lookAt 点而非原点），screenToWorld/worldToScreen 射线实现本就姿态通用不用动；stageManager.test.js 中心点断言改锚 lookAt + 单调性。
   - **坡面地板→水平地板**（用户：法线朝镜头倾斜是大问题，按真 3D 常理建场景）：FLOOR_Y=-30 常数（`floorY()` 保留函数形），地板 z∈[-45,+95] 近端伸出底缘；柱高 56→48（帽顶 ≈+20 低于眼高）；吊灯 42→22（不被顶边裁掉）；门/碎石/浮尘贴新地面。
   - **slotTransform lane 偏移改地面 XZ 法向**：旧 XY 法向会抬离地面。取 `(dz,-dx)` 向——lane+ = 屏幕左上（后排）、lane- = 屏幕右下（前排），与旧假透视屏幕语义一致（瑞米 lane<0 在主角右前方）。**laneGap 9→16**：法向偏 z 向，透视下屏幕分离 ≈ Δz×sin(俯角)，9wu 只分 2wu 屏距、相邻敌人前后串叠（拖牌测试打到前排 e0 而非目标 e1——后排下半身被前排遮是 2.5D 诚实遮挡）。战线轴同步加宽：near(-50,y,22) → far(70,y,-34)（友军 ~20-40% 屏宽、敌军 ~60-85%，AP/魏启点让出中路）。
   - **UnitObject 贴地化**：脚底锚点=地板。阴影/目标金环 rotation.x=-π/2 平贴地面（微抬 0.15/0.25 防 z-fight）；HP 条从脚下（会被地板裁掉）上移叠脚踝前方（y=+3.4、z=+0.6），标签 y 钳 `h/2-3.1` 不沉进地板。
   - **手牌扇压低** centerY -35→-40：近景卡牌（z≈30）顶部原在 64% 屏高，正好盖前排敌人血条；压低后卡顶 ~69%，敌血条（~60%）无遮挡。
   - **光照两轮调参**：初改后全场洗橙（火把 2600 距离 120 太霸道）→ 过暗（1500 时近景地板全黑、门不可见）→ 定版：火把 1900/距离 95、吊灯 1250/95、环境光 0x3a4260×1.5、补光 0.75、墙/地板材质加深；**火把 4→8**（前排柱×2 照友军区与近景地板、门侧壁灯×2 做远景焦点）；雾 (245,330)→(215,320)。吊灯铁环靠环内点光照亮（matIron 提亮 0x55555e），截图确认环/链可读。
   - **调试记录**：①playwright 里重复 `bridge.start()` 会 abort 首场战斗 → 调试页常驻"战斗结束：null"横幅——页面已 auto-start，别重复 start；②picker.test.js 的 `toScreen` 线性近似硬编码轴对齐映射 → 改走 `sm.worldToScreen`（与 battleStage 同款）；③dungeon3d.test.js 契约更新（8 火把/9 点光/火焰锚点 z 集合断言）。
32. ~~**牌桌 UI 独立渲染 pass（uiScene）**~~ ✅ 2026-08-01（233 绿 + playwright 验证）。
   - **问题**（用户发现）：卡牌 z-test 在地板之下——手牌世界坐标 y≈-40..-53 位于地板平面（y=-30）以下，而地板近边伸到 z=+95 已覆盖整个画面底缘，相机到卡牌的射线先打地板，卡面透明材质默认 depthTest → 下半截被裁。
   - **方案**（用户定）：卡牌单独开 world 渲染。StageManager 支持 `stage.uiScene`——tick 里先 `render(scene)`，再 `autoClear=false + clearDepth() + render(uiScene)`（假 renderer 无 clearDepth，可选调用守卫）。UI 与 3D 世界不做深度交互，永远前景。
   - **BattleStage 路由**：uiScene = 卡牌（手牌/咏唱/离场飞行/查看器）、按钮、牌库坟墓图标、资源点；世界 scene 保留 = 场景 3D、单位、粒子（伤害数字在地板上方，需要与场景正确深度交互）。**坑：Picker 拾取前 `updateMatrixWorld` 要刷两个场景**（卡牌挪走后单测不渲染，matrixWorld 不自动刷新）。
   - playwright 验证：卡牌/图标完整渲染无裁切；真实鼠标拖冲拳 → 敌人 18→12 交互链完好。
33. ~~**斜方向俯视（azimuth+大俯角）+ 双相机（UI 专用正直相机）+ 单位球面 billboard**~~ ✅ 2026-08-01（233 绿 + playwright 截图/交互验证）。
   - **世界相机斜向化**（用户定）：`CAMERA_AZIMUTH=-14°`（**右侧**看向场景——首版写成左侧被用户当场打回，符号 = position.x 的符号）、`CAMERA_ELEVATION=24°`（俯视幅度加大，原 ~11°），lookAt (0,-15,0) 不变，距离公式不变（z=0 平面尺度不变）。
   - **双相机**（用户：卡牌和牌桌 UI 总是需要正对相机）：UI pass 配专用正直俯相机 `uiCamera`（position (0,30,235)→lookAt (0,-15,0)，即斜向化前的旧相机——**全部 UI 布局坐标都在它下面调过，零返工**）。三链路分流：渲染（uiScene 用 uiCamera）、拾取（Picker pickable 带 `space:'ui'`，**UI 命中优先于世界命中**，与"清深度后渲染"的覆盖次序一致）、拖拽反投影（`screenToWorld(px,py,planeZ,cam)` 加 cam 参数，拖拽/出牌线判定传 uiCamera）。
   - **单位立牌形（圆柱）billboard**：standee/hpBar/fxAnchor 装进 billboard 子组，`faceCamera(camDir)` 逐帧 yaw=atan2(dx,dz) 绕脚底锚点水平转向相机；阴影/金环贴地不参与；呼吸/受击仿射在 standee 子组上不受影响。**首版做成球面 billboard（带 pitch=-asin(dy/len) 后仰正对视线），用户看效果不佳（纸片后仰倒地感）→ 改只 yaw、立面垂直地面。**
   - **布局后果**：UI 正直投影 + 世界大俯角 → 前排（z>0）单位下半身被手牌扇遮住——瑞米槽位 lane -0.9→+0.9（主角右后方）。**友军槽位一律后排**写进 STAGE_DESIGN §1。
   - 测试适配：stageManager 常量改 `CAMERA_LOOK_AT` 对象 + 透视纵深断言改"近远投影分离"（斜相机下轴向启发式失效）；battleStage/zonePiles 测试 helper 分 `toScreen`（世界）/`toScreenUI`（uiCamera）；picker.test.js 无 uiScene pickable 自动走世界相机（space 缺省 world）未动。
   - playwright 验证：右侧斜视构图（右柱近大左柱远小）、敌我沿对角线纵深排开、立牌全正对无压斜、卡牌/按钮平直正对；真实鼠标拖冲拳 → 中途 `_dragTargetId`=u2、敌人 18→12。
34. ~~**出牌交互双模式（杀戮尖塔式瞄准 + 旧拖拽兼容）**~~ ✅ 2026-08-01（237 绿 + playwright 双模式实机验证）。
   - **痛点**（用户）：拖牌选目标时卡牌盖住目标看不到属性。**方案**：按声明式 `targetMode` 分流——
     ①技能定义加 `targetMode:'enemy'|'none'`（缺省 'none'；冲拳/点火='enemy'），`projectCardFull` 投影
     （纯前端交互声明，结算语义不变：白名单目标+默认选靶）；②`'enemy'`=StS 式瞄准：卡**留手牌原位**
     高亮+撑开（复用 hover 布局，`layoutHand` 的 spreadId 传 aiming id），`TargetingArrowObject`
     （新，`src/stage/objects/`）圆点曲线追随指针——二次贝塞尔向上凸、14 圆点尾小头大、末端三角
     箭头沿切线，掠过存活敌人时箭头暖金→青绿 + 单位高亮；**松手在敌人才打出，否则取消（过线也不打）**；
     ③`'none'`=旧拖拽不变（卡随指针走 z=30，过 PLAY_LINE_Y 打出）。
   - **瞄准箭头是 UI pass 覆盖层**（z=45 高于手牌扇、depthTest:false、renderOrder:5；node 单测无
     document → 圆点贴图 null 方块占位）。指针追随物，不进队列/注册表。瞄准中资源点"即将消耗"
     高亮保持（`_updatePendingPips` 读 aiming id 优先于 hovered id）。
   - 测试：targetingArrow.test.js（曲线排布/变色/零距离不崩）+ battleStage 改写 3 用例（格挡旧拖拽
     过线打出卡随指针走 / 冲拳瞄准过线松手=取消 / 瞄准锁定敌人变色+高亮+落伤）。
   - playwright 验证：冲拳瞄准中途（金点曲线+卡留手牌撑开高亮）→ 锁 u3（绿弧+目标金环）→
     松手 u3 30→24、AP-1；格挡旧拖拽过线 → 护盾+5。
   - **后续做需选目标的新卡记得在技能定义标 `targetMode:'enemy'`**（STAGE_DESIGN §3.5 已收录契约）。
35. ~~**场景重设计：高塔内景 + 永夜 + 统一冷调（自由发挥轮）**~~ ✅ 2026-08-01（237 绿 + playwright 两轮截图调参）。
   - **用户设定**：高塔之内、塔外永夜、全场统一冷调——**火把也得是黄绿/冷白色幽火**；几何可更丰富/不对称，不要纹理细节。
   - **dungeon3D.js 大改**（build3D 契约不变：{group, torches×8, update, sampleStandeeTint}）：
     ①尖拱长窗×3（pointedArch Shape + 外拱套内拱石框 + 窗域内星点；中央高窗悬冷月+辉光晕、左窗半砌死、右窗矮窄）；
     ②月光柱×2（additive 冷色光幕斜落地板）；③尖拱 doorway 取代木门；④扶壁×3 不对称 + 破幡×2（update 轻摆）；
     ⑤**柱列不对称叙事**：右列中柱断裂（残桩+斜茬顶+倒柱段+柱帽残块+小碎块簇），左列三根高低错落；
     ⑥错位石板×10 + 裂板一对；⑦幽火壁灯（黄绿外焰 0x9ed85e + 冷白内焰 0xeaf6e2 + 火盆碗，残桩低挂一只）；⑧吊灯偏心 + 冷白烛焰。
   - **光照再平衡**：幽火点光 1500→1150、distance 90→78（绿池只留灯周一圈，否则全场洗绿盖过冷蓝——第一轮截图的教训）；
     月光平行光 0x8fa8d0×1.05 自窗向泻入；环境光 0x2c3852×1.6；雾同步深夜蓝黑 0x060a14（BattleStage）。
   - **sampleStandeeTint 改黄绿主导**（r+0.20f / g+0.48f / b+0.10f，冷蓝底 (0.50,0.56,0.74)）——立牌染色与场景冷调同源；
     dungeon3d.test.js 染色断言从"暖 r>b"改"幽火 g 主导 / 无火 b 主导 / 任何处 r 不主导"。
36. ~~**场景五轮：蓝白紫调 + 左墙穿窗月光（shadow map + 体积光幕）+ 正面素墙**~~ ✅ 2026-08-01（237 绿 + playwright 六轮截图调试）。
   - **用户反馈**：四轮太绿 → 偏蓝白紫（绿再压）；**左侧要一面墙，月光从左侧斜射而入**（可 shadow map + 体积光）；
     正面改素墙（砖块/门/旗帜点缀）。
   - **调色板迁移**：幽火黄绿→蓝紫（外焰 0x7a8ff0/内焰 0xeef2ff/点光 0x7488e8/粒子淡紫），石材/环境光/雾全部泛紫蓝；
     sampleStandeeTint 改 **b 主导**（r+0.16f/g+0.20f/b+0.48f，底 (0.52,0.55,0.75)）。
   - **左墙（x=-95）月光系统**：①分段盒子拼真实墙体开两扇尖拱窗洞（winA 大低主光路、winB 小高），全墙体 castShadow；
     ②**StageManager.attach 开 shadowMap（PCF，PCFSoft 新版弃用会警告；假 renderer 守卫跳过）**，
     月光平行光 0x9db4ec×2.2（mapSize 2048）穿窗在地板投窗形光池——**环境光 1.6→1.25、补光 0.45→0.35 才有对比度**；
     ③窗外永夜背板 + 星点 + 冷月辉光晕——**星簇/月亮钉死在窗洞视线切片上**（相机固定，cam→窗心延长线与背板求交；
     随机撒星在窄切片里必然一颗都没有）；④**体积光幕每窗两片**（竖幕含光向与窗高=主视觉、横幕含光向与窗宽=辅），
     渐变 alphaMap + 呼吸。**调试实录**：a) 月亮 initially 被正面墙挡住——旧正面墙 280 宽探过左墙（x<-93），
     射线调试（raycast 命中序列）揪出，正面墙左端必须止于左墙内侧面；b) **three 的 alphaMap 读绿色通道——
     渐变画在 alpha 通道上无效（canvas 像素 RGB 不随 alpha 变），必须画黑白亮度 + multiply 合成**；
     c) 页面内投影调试法：`sm.worldToScreen` 验证月亮与窗心投影重合 + raycast 查遮挡链。
   - **正面素墙**：砖块补丁×2（逐砖抖动+缺砖）、尖拱 doorway 保留、破幡×2（紫/青蓝，暗墙上要提亮才读得出）、
     窄缝窗（暗槽+铁栅）；旧正面三窗/月亮/光柱全部迁移或删除。
   - 最终效果：冷月悬左窗（被柱影半掩）、双窗星点、斜射光幕、窗形光池、蓝紫幽火——永夜高塔氛围定稿。
37. ~~**真 ray marching 体积月光 + 程序化 skydome（推翻假光幕/背板）**~~ ✅ 2026-08-01（237 绿 + playwright 十余轮联合调试）。
   - **用户两轮 direction**：①假面片不算体积光，要 shadow map + ray marching；月亮不必在窗内，skydome 该程序化生成而不是贴几何背板；②后续逐条指认漏光机制（墙不够高/不够厚/相机前有共有路径/sp.z 卡界/立牌写假深度），并加了 frame jitter seed（%30 轮转）+ 要求 temporal history（EMA 1/30）。
   - **volumetricMoon.js composer**（StageManager composeScene/composeResize 钩子接入；假 renderer 回退）：
     场景→带深度 RT（HalfFloat+MSAA×4）→ march pass（26 步 jitter march + shadow 空间硬件比较 + **EMA 写历史 ping-pong RT，只 EMA 光项不 EMA 场景色**——全场 EMA 会让火焰/呼吸/卡牌动画拖影；首帧/resize 后 emaAlpha=1 直接定植）→ composite pass（场景色+EMA 光量，手动 pow(1/2.2) 补线性→sRGB，**自写合成 shader 绕过 three 输出链路必须补**）。
   - **skydome.js**：球壳 BackSide + shader（地平线暗紫→天顶渐变、3D 格 hash 星野、高空月亮+晕——与陡角月光物理一致），方向以相机为原点采样。五轮的几何背板/钉星/假光幕全拆。
   - **漏光/发奶调试实录（全部数学验证，极有价值）**：
     a) three r185 PCF 阴影深度在 `shadow.map.depthTexture`（UnsignedInt+compareFunction），**color 纹理是垃圾，别采 .texture**；采样用 sampler2DShadow + shadowCoord.z+bias（与 getShadow 同约定）；
     b) **light.position 是阴影相机深度原点**：墙在 y≈49.5 以上沿光轴深度为负被 near 裁掉→shadow map 缺上半墙→回溯打中上半墙的采样恒亮（右上角楔形，边界=y≈49.5 等高线，加高墙纹丝不动的根因）——position 沿光轴反向后移 120wu 即修复，不影响光照方向；
     c) **墙高三铁律**：高（墙顶须 > 最远可见空气回溯高 y+(x+93)*0.648，现 +460）、厚（>2 shadow texel，现 12wu）、z 宽（光向 dz/dx 漂移 ±25，现 [-75,+125]）；
     d) **march 起点截到房间近缘 z=95**：相机在房间外，相机→房间共有路径无墙可挡，不截全场发奶；飞出 shadow 覆盖按完全阴影处理且 sp.z 卡下界（负 z 做 LessEqual 恒亮）；maxDist=300；
     e) **立牌 alpha 材质改 alphaTest 0.5 二值 mask**（transparent+depthWrite 让全透明像素写假深度，体积光被矩形截断；用户定）+ **billboard castShadow 开启**（alphaTest 剪影投影，用户定）；
     f) 调试方法论：composer `_uniforms` 实时调参、红色可视化（lightColor=(1,0,0) 看光束 sculpt）、density=0 隔离基线、冻结 tick（`_unsubTick()`，注意 update 每帧重置光强）后 castShadow 开关差分 + PIL 块均值热图、参数扫描（maxDist/nearZ）+ readRenderTargetPixels 读 shadow map。
   - 定版参数：density 0.01、lightBoost 0.9、26 步、EMA 1/30（30 帧收敛，jitter 条纹被时间域抹平）。
38. ~~**粒子空间分流（伤害数字错位修复 + 逐粒子 size）**~~ ✅ 2026-08-01（239 绿 + playwright 实机验证）。
   - **问题**：伤害文本粒子出现在屏幕下方（错位）——旧 2D 时代硬编码 `z:70` 特效层，单位站在 3D 地板（z≈-30~+15），
     斜相机（az -14°/el 24°）下 z=70 比单位近几十 wu，投影偏移到屏幕下方；点粒子爆发同病（范围大不明显）。
     另有契约 bug：spawn 的 `size` 选项从未生效（PointsMaterial.size 是材质级全局值）。
   - **设计决策（用户拍板"按种类分流"而非二选一）**：**点粒子（火花/碎屑/火焰/浮尘）= 真 3D**（世界 pass，
     吃雾/深度/透视——是场景一部分，spawn 传单位实际 z）；**读数文本粒子（伤害/治疗数字）= UI pass**
     （uiScene 前景层——伤害数字是读数 UI 不是世界物体：恒定屏幕尺寸、不被柱子/地板遮挡、远敌人字不缩小；
     相机固定无"贴玻璃"违和；杀戮尖塔同理）。
   - **实现**：ParticleSystem sprite 池按 `space:'world'|'ui'` 分两套（`sprites`/`spritesUI` 两组，池位互不占额，
     死亡归还对应池；材质 fog:false——读数不吃雾压暗）；BattleStage `uiScene.add(particles.spritesUI)` +
     `_unitToUI(unit, dx, dy)` 桥接（世界相机投影到屏幕像素 → UI 相机反投影到 z=70 平面，spawnText 缺省
     z=70 恰落该平面无深度差；单测无视口尺寸时退化世界坐标直用保数值有限）；所有 spawn 点补实际 z
     （受伤/死亡/治疗爆发用 `unit.position.z`），文本粒子全部 `space:'ui'`。
   - **逐粒子 size**：aSize BufferAttribute + onBeforeCompile 补丁（`gl_PointSize = size * aSize`），
     材质 size=1 留全局乘子；构造 pointSize 改缺省、spawn options.size 逐次覆盖。
   - **验证方法论**：真实出牌路径的粒子捕获用 `waitForFunction` 轮询 `_spritePool.length` + hook spawnText
     打时间戳（燃烧 -2/直接驱动 -7 均正确锚定单位头顶）；5s 短窗口会被回合切换的多节拍排队吃掉，放宽到 15s。
39. ~~**护盾视觉层（保护框 + 盾徽 chip + 破碎碎粒）+ 伤害节拍分流**~~ ✅ 2026-08-02（247 绿 + playwright 实机验证）。
   - **护盾渲染**（UnitObject hpBar 内 shieldGroup，shield>0 可见）：蓝色保护框（微蓝背板+四细条 0x5aa8ff）
     包裹血条 + 左侧盾徽数值 chip（canvas 程序化盾形图标全局共享纹理 + 数值文本左锚定）；
     HP 主标签去掉"盾N"后缀。数值变更 chip 放缩跳动（`_shieldPopT` 0.28s 线性衰减 1.45→1，update 帧驱动，
     牌库脉冲同语言）；值不变不重烘（`_shieldBaked`）。
   - **破碎 vs 自然消失：区分在因果不在状态（关键设计）**。`>0→0` 状态检测无法区分"被打破"（伤害吸收）
     与"自然消失"（turn.js 回合开始直接清零，无事件）——曾试图走 UnitObject onShieldBreak 回调，用户指出后
     重构：**破碎碎粒只由伤害节拍驱动**（`_damageHit`：`shieldAbsorbed>0` 且 `_displayShieldOf(上一 sync
     快照) - absorbed ≤ 0` → `_shieldBreakFx` 蓝白碎粒 24+10 自血条迸射）；自然消失只是保护框随 sync 静默隐去。
     UnitObject 只负责随 sync 显隐/跳动，不知因果。
   - **伤害节拍按落点分流（用户定）**：dealt>0 才闪红+红色火花+击退（节拍阻塞）；全吸收只有蓝色火花+
     灰色吸收数字，**不翻红不击退**，animator 空 tween 短停 220ms 即收节拍。
   - **测试坑**：①instantTween 同步播完击退链会把 flash 窗口又被 restoreColor 关掉——断言闪红要 spy
     `unit.flash` 调用次数而不是查 `_flashT`；②`make()` 的 deck 会洗牌（seed 1），`hand[0]` 不一定是
     第一张——按 `defId` 找卡（chant 测试同款写法）。
40. ~~**状态绘制独立于场景深度（renderOrder 60+ 浮层）**~~ ✅ 2026-08-02（248 绿 + playwright 遮挡实测）。
   - **问题**：场景遮蔽立牌是合理的，但场景有时也会遮蔽 HP 条等状态绘制（用户报）。
   - **方案**：hpBar 全家（底槽/填充/数字）+ 护盾层（保护框/背板/盾徽/数值）一律
     `depthTest:false + depthWrite:false + renderOrder 60+`（UnitObject `statusify(mesh, order)` helper；
     层级：场景 0 < 状态 60~64 < 粒子 70/71；同层 depthTest 关闭后顺序靠显式 renderOrder painter 序，
     bg<fill<label、backfill<bars<chip）。depthWrite 必须关——否则往体积光 march 用的场景深度 RT 写假深度。
     卡牌等 UI 是独立 uiScene pass（清深度后渲染），天然仍在状态之上（用户要求"保持在卡牌 UI 下方"=免费）。
     立牌本体/阴影/金环不动，仍正常吃深度被场景遮蔽。
   - **验证**：页面内用现有对象构造器搭大平面（`new unit._body.geometry.constructor(...)`——浏览器里
     `import('three')` 裸标识符解析不了）挡在玩家立牌前，截图确认立牌被遮、盾徽+HP 条浮在遮挡物上方。
     单测断言状态件 depthTest/depthWrite 关闭 + renderOrder∈[60,70) + 递增 painter 序 + 立牌本体仍吃深度。
41. ~~**UI pass 切换独立正交相机（透视畸变统一）**~~ ✅ 2026-08-02（249 绿 + playwright 实机验证）。
   - **问题**：切透视相机后卡牌/交互 UI 世界位置错乱——透视 UI 相机让不同 z 层吃不同投影缩放/偏移
     （咏唱槽 z=4 vs 手牌 z=20+ 错位），用户拍板"新增单独 ortho 相机"（优于统一到世界相机）。
   - **方案**：`uiCamera` = OrthographicCamera 正视 -z（position (0,-15,500)），布局坐标↔屏幕**线性映射**，
     z 只定前后层不改投影；拾取/拖拽反投影随之线性，一类问题全消。可视范围 y∈[-65,+35]（底部留手牌构图）。
   - **视锥双计坑（关键）**：取景中心偏移只能由相机位置承担——视图空间视锥必须对称 ±50；
     第一版把 top/bottom 也偏移成 [-65,+35]，等于中心算了**两遍**（按钮渲染位置与投影数学不符，
     页面内 worldToScreen 对账揪出）。
   - **咏唱槽列顶 32→18**：正交取景上限 y=35，卡半高 13.5 → topY≤21.5 不被上缘裁
     （旧透视 UI 相机在 z=4 平面可视范围更大，topY=32 当时恰好没事）。
   - **测试债还清**：hover 手牌测试一直误用世界相机投影（旧两透视相机碰巧接近能中），
     正交后发散 → 改 toScreenUI；新增 stageManager 正交映射断言（线性/z 无关/互逆/取景边界）。
42. ~~**单位效果行（fxRows：icon+特征色名称+极性层数 + hover tooltip）**~~ ✅ 2026-08-02（256 绿 + playwright 实机验证）。
   - **数据**：`projectUnit` 把效果定义元数据压平进投影（name/type/color/icon，经效果注册表
     反查，未注册按 id 兜底）——Stage 不 import Core 注册表的约定延伸到效果。
   - **呈现**：血条**上方**左对齐纵列（hpBar 内 fxRows，随 billboard 转向），每行 =
     暗背板 + richtext 烘焙（`icon + /特征色{名称} + /绿或红{xN}`；特征色=效果定义 color
     字段即 richtext 颜色名；buff 层数绿 / debuff 层数红）；行自下而上堆叠，签名（含层数）
     驱动整列重建，HP 变化不重烘。HP 主标签的效果文本占位（`effectId xN` 后缀）已删。
   - **tooltip 复用 token 协议（零新协议）**：行网格带 `userData.effectRow`
     （`{ type:'effect', payload:{ name } }`，与卡面 hitRegion 同构）；Picker 对
     kind:'unit' 命中做二级查询返回 token 命中 → hover 走既有 `tooltip:show/move/hide`，
     debug shell 按 name 反查定义弹 popup（该 kind:'effect' 分支原本就存在）。
     **kinds:['unit'] 过滤（拖牌/瞄准）跳过二级查询**——效果行区域仍是合法出牌落点。
   - 渲染层级：statusify 同规则（depthTest/Write 关闭，renderOrder 65/66，场景上粒子下）。
   - **存量测试债清理**：HEAD 上 4 个用例红（上轮牌库/坟墓图标挪位 76,-35/-13 →
     80,-55/-38 时漏改测试与布局头注释），已同步修正，与本次特性无关。
   - fxAnchor（头侧图标列）继续留位；图标化精简版（仅图标+角标）留作后续迭代。
43. ~~**烘焙文本抗锯齿（效果行锯齿修复）**~~ ✅ 2026-08-02（256 绿 + playwright 截图对比验证）。
   - **根因**：状态文本用 `alphaTest:0.5` 二值 mask，把 canvas 烘焙自带的 AA alpha 渐变
     砍掉 → 阶梯锯齿；MSAA（renderer antialias / 体积光 RT samples=4）只管几何边，
     管不了纹理内容。
   - **修复**：① 状态层文本材质（HP 数字/盾徽/盾徽数值/效果行）改 `transparent:true`
     真 alpha 混合（statusify 本就 depthTest/Write 关闭 + 显式 renderOrder，painter 序
     确定无排序风险；standee 立牌吃深度仍 alphaTest）；② `_bakeLabel` scale 2→3
     （更干净的 mipmap 链）；③ 烘焙纹理 `anisotropy = min(8, max)`（效果行随 billboard
     与俯视相机成斜角）。
   - **TAA 评估（不做）**：TAARenderPass 需 jitter+velocity/history，对快速粒子/伤害
     文本产生拖影，且与体积光 EMA 历史叠加双重时间滤波；FXAA/SMAA 最终 pass 会软化
     小字——纹理内锯齿应对症下药用光栅质量（混合+超采样+aniso），后处理 AA 不是正解。
   - **遗留可选**：renderer 未 setPixelRatio，dpr>1 屏（Windows 缩放 125%/150%）整体
     会软；要做需同步体积光 composer 的 RT 尺寸（raymarching 成本随 dpr² 涨，建议 cap 2）。
   - 用户实时微调：效果行层数格式 `x3` → ` 3`（去 x 前缀），测试已同步。
44. ~~**战场布局改前-后占位（纵深构图）+ 场景拉长**~~ ✅ 2026-08-02（256 绿 + playwright 五轮截图调参）。
   - **战线轴转纵深**：battleLine near (-32,FLOOR,+15) → far (24,FLOOR,-46)——轴沿 z 走，
     友军前排近镜头偏左、敌军后排远镜头偏右（旧左右占位 (-50,22)→(70,-34) 废）；
     nearScale/farScale 1.08/0.85 加强近大远小。相机不动（azimuth -14/elevation 24 仍适用）。
   - **场景拉长**：FLOOR_FAR_Z -45→-80（远墙/扶壁/门/幡随常量自动后移）、左墙 z0 -115、
     柱列左右各 +1 深远柱、幽火 8→10（doorway 对随迁 z=-77）、吊灯后移 ( -6,20,-32)、
     石板 +3 深远块、浮尘 z 范围加宽、雾 215/320→165/310 让远排沉进暗部。
   - **前后排槽位经验（截图+投影对账得出）**：① lane+（屏幕左上）+ t 微差在屏幕上几乎是
     纯竖直位移——瑞米 lane+0.9 会正好躲进主角身后被挡死，同排并列要用 lane-（屏幕右=
     朝敌一侧，t 0.02 lane -0.75）；② near 排 z>15 时下半身被手牌扇遮（UI 覆盖层顶线
     ≈70% 屏高），前排脚下留边只能用 z 调；③ 敌排 4 槽 lane -1.4/-0.1/1.2/2.2 + t 微交错，
     斜向排开破直线感。
   - dungeon3d 测试同步：火把 8→10；染色采样"暗角"改真暗角 (120,-60)、幽火旁采样改
     未饱和区 (0,-25)（柱列火把 XY 投影堆叠，正上方采样顶满 1.12 clamp 失去比较意义）。
   - STAGE_DESIGN §1 battleLine 条款重写（含槽位经验）；顺手修正 §1 相机距离陈旧值 235→123。
45. ~~**牌面 /effect{} 渲染修复（emoji 图标 + 特征色名称文本）**~~ ✅ 2026-08-02（257 绿 + playwright 验证）。
   - **问题**：`/effect{燃烧}` 只画通用橙色徽章，名称文本完全不渲染（layout 只 placeIcon），
     图标与效果定义 emoji 不对应。
   - **修复**：layout effect token 与 skill token 同构——placeIcon + placeText（名称），
     两段都产 hitRegion；新增 `resolveEffect(name) => {color}` 注入（特征色，支持颜色名
     经 colorTable 解析或 css 直给）。cardFace 默认 `drawCardIcon`：effect 有定义 emoji
     画 emoji（`allEffects().find(d => d.name === name)`，Stage→Core 查表是允许方向），
     无图标回落 defaultDrawIcon 徽章；`DEFAULT_COLOR_TABLE` 从 layout.js 导出共用。
   - tooltip 不受影响：热区 type/payload 不变（两段都指向同一效果名），playwright 实测
     图标段/文本段 hover 均正确弹出、leave 正确隐藏。
   - parser.js 头部语法注释同步（/effect 语义=图标+特征色名称文本）。
46. ~~**场景丰富（保持永夜冷调基调）+ 相机/槽位用户手调定稿**~~ ✅ 2026-08-02（257 绿 + playwright 截图迭代）。
   - **用户手调（工作树实时改，agent 不得覆盖）**：相机 azimuth -14→**-34**、elevation 24→**20**
     （更侧更平，前后排纵深更强）；battleLine far z -46→**-60**、laneGap 15→**30**、
     nearScale/farScale 回 **1/1**（透视纵深已够，风格化夸张关闭）；瑞米 t 0 lane +0.75
     与主角并列。后续文档/测试一律以这组值为准。
   - **新增场景件**（dungeon3D.js，全程序化、冷调不破坏）：顶部拱肋×3（大半径薄管弧
     暗示穹顶，**castShadow=false**——平行月光下横拱会在地板光池拖横穿阴影）、
     右列深远柱+断楣两段、**立地火盆×2**（三脚架铁盆中景填空，入 torches 数组复用
     闪烁/粒子链路，火源 10→12）、吊链×2 + 废弃吊灯（斜挂无火）、doorway 内踏步×3、
     墙脚碎石堆×3、**月光束浮尘**（沿 winA 窗洞→光池光路体积 spawn 亮冷蓝缓漂尘，
     卖体积光；rate 3.6/s、size 0.75）。
   - dungeon3d 测试：火源 10→12、点光 11→13。
   - STAGE_DESIGN §1 相机/battleLine 条款按用户定稿值重写；§2 场景清单/雾距/火源数同步；
     左墙 z 覆盖 -75→-115 修订。
47. ~~**月光浮尘云重做（噪音场 + shadow map 真值着色）+ 假 GI 提亮**~~ ✅ 2026-08-02（257 绿 + playwright 截图迭代两轮）。
   - **GI 问题结论（用户问）**：three 无内建实时 GI 也无烘焙管线（`lightMap` 只是能贴
     外部烘焙图，要第二套 UV + 外部烘焙器，不适用）。廉价替代 = **HemisphereLight
     （球谐渐变环境光，假 GI 经典）+ 光池假反弹点光**（最出 GI 感、成本≈零）；
     SSAO/GTAO 后处理对平色低多边形收益存疑，不上。
   - **`src/stage/scenes/moonDust.js`（新）**：常驻浮尘云取代旧"沿光路参数化 spawn"
     （用户：位置对不上光柱）。1500 粒常驻尘埃场，全 GPU——顶点：手搓 hash 值噪声
     三轴漂移（无 simplex 依赖）+ 逐粒速度差缓慢下落（盒内回绕）；片元：世界位置
     经 `light.shadow.matrix` 变换后 **sampler2DShadow 硬件比较采样**（与 volumetricMoon
     march pass 同约定：飞出范围=完全阴影、sp.z 卡下界、采 `shadow.map.depthTexture`
     非 color 纹理）——光柱内亮冷蓝（uLit 0xcfe0ff×1.7）、阴影区极暗（0x16203c×0.5，
     加色混合下近不可见，兼作环境尘暗部空气感）。密度策略：40% 偏向两条光柱体积
     spawn 保光柱密度，wander 漂出真实光柱的被 shadow 采样自动调暗（边缘是真值裁切
     非参数近似）；60% 均匀填房间体积。CPU 每帧只推 uTime + 同步 shadow uniform，
     **shadow map 未就绪（首帧）整朵云 visible=false**。
   - **提亮**：AmbientLight(0x2e3442×1.25) → **HemisphereLight(0x3a4666 天光/0x232030
     地面 ×1.5)**（朝上面吃冷蓝紫、朝下面吃暗紫反弹，垂直渐变比平底 ambient 多一层
     体积感）；**光池假反弹点光×2**（0x8298d4，620/380 candela，贴 winA/winB 光池
     上方 y=FLOOR_Y+9 朝上打，把柱列/墙面当月光二次反弹面，暗部不死黑；无阴影
     零额外 shadow map 成本）；补光 0.35→0.5。点光总数 13→15（dungeon3d 测试同步，
     新增 moonDust 存在+初始隐藏断言）。
   - 旧 DUST_RATE/MOTE_RATE spawn 块全删——ParticleSystem 只留火焰粒子；浮尘测试名
     同步改"火焰粒子从火把锚点发射（上浮）"。
   - playwright 验证：光柱内浮尘成簇可见（两窗光路被柱影切碎的亮暗分布与体积光一致）、
     暗区微光空气感、1.5s 双截图 diff 0.72% 像素变化确认漂移/下落动效在跑、全场亮度
     提升单位可读性改善。
   - STAGE_DESIGN §2 光照/氛围粒子条款重写；§5 落地顺序补第 7 条。
48. ~~**光照焦点：战场主补光（环境光与主光职责分离）**~~ ✅ 2026-08-02（257 绿 + playwright 截图验证）。
   - **问题**（用户）：把 HemisphereLight 调到 ×14.5 想提亮，但它是**全局光——
     没有位置也没有衰减**，提亮必然全场均匀洗亮，做不出"焦点在战地、周围暗"。
   - **方案**：全局环境光只当保底（压回 ×3.0 防暗部死黑），光照焦点交给**战场主补光
     battleGlow**（0x93a5d8、8000 candela、distance 260、decay 1.8）——悬在战线轴
     中点 (-4, FLOOR_Y+60, -22) 正上方的大点光，物理衰减让亮度向远墙/近景画外
     自然跌落。一盏无阴影点光，成本≈零。
   - **强度估算方法**（下次调灯可参考）：three 物理衰减 I/d^decay——火把 1150cd
     在 15wu 处≈8.8；主补光距地板 60wu 要贡献同级亮度需 ≈5×60^1.8≈8000cd。
   - 点光 15→16（dungeon3d 测试同步）；截图确认中心光池自然渐变、远墙压暗、
     敌我单位与血条可读。
   - STAGE_DESIGN §2 光照条款重写；§5 落地顺序补第 8 条。
49. ~~**玩家状态栏（左下角：头像 + AP/魏启 + 能力留位）**~~ ✅ 2026-08-02（+1 测试文件 +5 用例，共 **41 文件 262 绿** + playwright 截图/真鼠标验证）。
   - **`src/stage/objects/PlayerStatusObject.js`（新）**：圆角深色底板（canvas 烘焙
     微渐变+细描边，node 退化纯色半透明 plane）+ 圆形头像（CircleGeometry，
     `setAvatar(image)` 用 **texture repeat/offset 做方形裁切**取头顶部区域——
     CircleGeometry UV=单位圆外接正方形，repeat.x·w_px = repeat.y·h_px 等比不变形，
     无需 canvas 合成）+ 金属描边环 + AP/魏启资源点两排 + **`abilities` 留位 Group**
     （精英能力/大师能力灵脉图标行的未来挂载点，布局常量 `PLAYER_STATUS_LAYOUT` 集中
     在文件内导出）。
   - **ResourcePipsObject 加 `align:'left'`**（默认 'center' 不变）：左缘锚定排原点，
     状态栏多行左对齐用；高亮脉动/消耗弹跳/颜色渐变帧驱动逻辑零改动。
   - **UnitArtCache 加 `getFile(file)`**：按文件名直接取图（头像用 unit_player_front.png
     正视图——"unit_xxx.png=舞台用图 / unit_xxx_front.png=正视备用"命名约定的首个
     消费方），加载/补挂语义与 get() 相同（BattleStage `_applyAvatar` 挂接，
     onLoad 时随 `_applyUnitArt` 统一补）。
   - **BattleStage**：状态栏摆 (-70,-59.3,z=6)（UI 底缘 -65 与手牌底缘 -53.5 之间，
     z 低于手牌 10+，左侧手牌 hover 盖住面板右上一角可接受）；`_resources.ap/mana`
     引用不变（reconcile/tick/测试全照旧），仅父级从 uiScene 换成状态栏；
     dispose 改走 `_statusBar.dispose()`（防双重销毁）。
   - playwright 验证：头像裁切出头肩比例正确、两排点整齐；**真鼠标悬浮 1AP 格挡 →
     末尾 AP 点脉动泛白、魏启不动**（高亮联动在新位置完好）。
   - STAGE_DESIGN §1 双 pass/布局条款补状态栏段落。
50. **卡牌养成定调：4+1 等阶、废弃 powerUp 轴**（2026-08-02，设计决策，已写入 SKILL_DESIGN_PRINCIPLES.md"养成与等阶"节）。
   - **决策**：等阶砍到 **D/C/B/A 四档晋升阶梯 + S（特级卡）+ Z（诅咒卡等）单列**（均不可晋升；S=稀有强卡/Boss/转化产物，Z=负担卡）。旧 11 档（C-/C+/B-/B/B+/A-/A+ 细分）作废。升阶=局外 Transform 换 def，系列用 `series` 串联，晋升允许分叉。相邻档必须是机制跃迁非数值微调（旧姿态链为范式）。
   - **powerUp 轴废弃**：卡牌定义保持纯静态；局内数值漂移未来走卡牌 modifier（未实现），局外永久成长只有升阶。
   - **待做手术（随卡牌 modifier 落地一并做）**：core/bridge/stage 仍残留 power——`state/skillRuntime.js` power 字段、`instructions/cards.js` TransformCardInstruction keepPower、`content/skills.js` 冲拳 `sctx.self.power`、`bridge/projection.js`、`stage/richtext/cardFace.js` 威+badge、`BattleStage.js` powerUp 金色脉冲（:327-336）；测试侧 test/deckCraft.test.js（锻刀+3）、test/bladeCombo.test.js（断神斩衰败）、test/transform.test.js（锈刀+keepPower）、test/battleEffects.test.js（powerUp 脉冲）、test/state.test.js、test/skills.test.js。断神斩/锈刀的"渡回合衰败"机制本体保留，数值载荷届时换 modifier。
   - 后续话题（未定）：晋升点/晋升素材的经济设计、晋升分叉的具体形态。
51. **全局流程与养成体系定稿**（2026-08-02，已写入 **`quest_prompts/RUN_DESIGN.md`**——run 层 core 基建的依据文档）。
   - **核心循环**：线性爬塔无岔路；战斗 → 固定奖励（金币+技能3选1）→ 奖励房四选一（老虎机/训练场/营地/事件，按楼层+种子）。
   - **跨战斗资源**：生命值与**魏启均跨战斗持久**（已定：魏启战斗开始不回满——现行 `battleRoot.js:23` 每场回满需改，会牵连全部战斗测试的蓝耗预期）；无药水。
   - **进阶经济（已定）**：主途径=训练场训练次数达标；老虎机可出"训练次数"奖励但效率低于训练房。进阶效果：全恢复+魏启上限提升+卡池等阶门禁展开+开放升阶。能力授予：进阶达标后 `pendingAbilityGrant` 置位，下个事件房选择精英/大师能力。
   - **架构原则**：run 层=纯确定性状态机，**不套结算指令树**（指令树仅战斗内）；`src/core/run/` 模块划分、RunState 重构字段、遗物注册表（镜像 ability 契约）、`promotesTo` 晋升映射、瑞米 `createUnit(level)`+`supports` 契约、技能 spawn 元数据搬运、RunDriver SDK，均见 RUN_DESIGN §6。
   - **战前准备补充（同日定）**：遗物有**装备上限**（`relicSlots` 初始 2，一局获得 5~6 个需装卸，仅装备中挂载战斗钩子）；新增**战前准备阶段**（地图界面：水彩高塔+星空，**three.js 舞台渲染**——属"舞台"非剧情幕间，已定）——预告下战敌人（遭遇在战前按 seed+floor 确定性生成）、装配遗物、安排瑞米；遗物契约预留 **`prepUse` 战前主动钩子**。RunState 加 `relics/equippedRelics/relicSlots`。
   - **故事模式接缝（已定，只留缝不建管道）**：`createRun({ seed, profile })`，瑞米状态=profile 基线+局内增量；run 状态只存 id 与数字保可序列化。
   - 等阶体系最终定稿 **D/C/B/A 阶梯 + S（特级）+ Z（诅咒）**，SKILL_DESIGN_PRINCIPLES 与 handoff 第 50 条已同步。
52. **全局流程疏漏审查与补裁（2026-08-02，全部落入 RUN_DESIGN.md）**。审查发现的最硬冲突：`turn.js:59` **每回合回满魏启**——只改战前回满无法让"跨战斗持久"生效。用户裁定：
   - **魏启无任何自然恢复**（战前不回、回合不回；`battleRoot.js` + `turn.js` 两处都要改），回复仅营地/进阶/卡牌手段；回复稀缺是刻意基调（全局资源规划）。
   - **塔结构**：44 层 = 4 章 × (10+1)，Boss 层 11/22/33/44；胜利=杀穿存活；败=run 结束。
   - **训练房固定 4N-3 层**；无可升级卡时改为可选抓牌；**离开训练房达标直接进进阶事件**（废除 pendingAbilityGrant 延后机制）。
   - **等级体系（用户纠正，勿误读）**：玩家=**四维独立等级**（火/木/空灵脉 + 体修），维度互不相干；**体系内等级达标才能升该体系卡到对应等阶**；5 附系等级=四主维度等级**加和**（均衡发展也是打法）。**总进阶次数 ≤6**（四维总和封顶 6，等阶门槛按此尺度标定；需支撑"三系各 B 阶"打法）。进阶事件内选一条主维度升级。RunState 用 `leino:{fire,wood,air,body}` 取代 ascension/unlockedTiers/pendingAbilityGrant。
   - **删卡**：Boss 奖励各删一张 + 部分事件；明确不做原生小卡组玩法，以局内过牌/弃牌/焚牌补足。
   - **瑞米**：HP 归零=被打跑，不再出战，营地找回。
   - **营地保底**：每个 Boss 战前必出现（细则留坑：训练层 33 与 Boss 层 33 碰撞等待调度细化）。
   - S 卡走事件投放（中后期内容后议）；Z 投放途径留坑。

**当前阶段：斜视相机 + 永夜冷调高塔场景（真体积月光）上的完整单场战斗（前-后纵深布局，用户定稿机位）。** 战斗舞台交互已搞定；卡牌养成（等阶 4+1+1、弃 powerUp）与全局流程（RUN_DESIGN.md，含疏漏补裁）已定稿。**下一步按 RUN_DESIGN §8 落地顺序**：① 魏启无自然恢复（battleRoot + turn 两处 + 修测试）→ ② RunState 重构 + run rng + 楼层推进（44层/4章）+ RunDriver → ③ 战后奖励+卡池生成 → ④ promotesTo+训练场（4N-3）→ ⑤ 进阶事件（四维等级+等阶门禁+能力授予）→ ⑥ 营地（Boss前保底）+老虎机+瑞米 → ⑦ 遗物注册表+战前准备 → ⑧ 地图界面（three.js）。舞台侧遗留候选不变：攻击者 lunge/死亡倒地动效 / Shell 薄壳 / 焚毁溶解 shader / 场景进阶 / Bridge 补强 / 并发动画组合（waitTags:[] 并行编排未被使用——记得 sequencer 是 command queue）。

## 换机续作 checklist

1. `git fetch && git checkout rewrite && git pull`（本文件与全部代码在 `rewrite` 分支）。
2. `npm install && npm test` 确认 262 绿（若 vitest 报 rolldown native binding 缺失，删 `node_modules` 与 `package-lock.json` 重装——npm optional deps bug）。
3. 读 `quest_prompts/THREE_REFACTOR_PLAN.md`（重点 §0 决策记录、§2、§11）。
4. 从上文"下一步"继续。
