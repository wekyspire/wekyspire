// propKit 交互行为模板目录（WORKFLOW §5，Phase 5 落地）。
// 规划：B.idleSway / B.flutterOnImpact / B.shatterOnBlast / B.lightExtinguish /
// B.torchFlicker——逻辑在 kit，参数在道具（资产侧 behaviors 只允许实例化模板）。
// P5 之前此目录为空占位：资产契约的 behaviors 字段一律保持 []。

export const B = Object.freeze({});
